#include <stdio.h>

/**
 * main - Prints the alphabetic
 * Return: Always (Success)
 */

int main(void)

{
       	char lowcase_alpha;
	for (lowcase_alpha = 'a'; lowcase_alpha <= 'z'; lowcase_alpha++)
		putchar(a);
        	putchar('\n');
	return(0);
}

